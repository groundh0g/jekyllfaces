{% capture jfCache %}{% comment %}

  Determine whether or not the site is being built in a production environment.

  Parameters:
    None.

  Returns:
    isProduction: [true|false]
    currentEnvironment: [development|github|other]

  Examples:

    {% include util-is-production.liquid %}

    {% if isProduction != true %}
      <h3>This is Private</h3>
      <p>I love to watch television in my undies. Don't tell anyone!</p>
    {% endif %}

    <h3>This is Public</h3>
    <p>I have no unusual quirks.</p>

{% endcomment %}

{% if site.github %}
  {% assign isProduction = true %}
  {% assign currentEnvironment = "github" %}
{% elsif jekyll.environment != "development" %}
  {% assign isProduction = true %}
  {% assign currentEnvironment = jekyll.environment %}
{% else %}
  {% assign isProduction = false %}
  {% assign currentEnvironment = "development" %}
{% endif %}

{% endcapture %}{% assign jfCache = nil %}